# Exclui artefatos de build e arquivos desnecessários do contexto Docker.
# O stage de build no Dockerfile compila do zero via Maven.

# Artefatos de build anteriores (o Dockerfile compila do zero)
therapist-scheduler-api/target/

# Controle de versão
.git/
.gitignore

# Arquivos do IDE e sistema operacional
.idea/
*.iml
.vscode/
.DS_Store
*.swp

# Documentação
*.md

# Dockerfiles internos do subprojeto (não necessários no contexto)
therapist-scheduler-api/src/main/docker/
therapist-scheduler-api/.dockerignore
therapist-scheduler-api/Dockerfile
